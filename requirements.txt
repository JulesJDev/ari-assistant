# ============================================
# Ari Assistant — Python Dependencies
# ============================================
# Core backend dependencies (no version locks per spec)
# ============================================

# Web framework
fastapi
uvicorn[standard]

# HTTP client (async)
httpx

# Text-to-Speech
edge-tts

# Environment & configuration
python-dotenv
pydantic
pydantic-settings

# Web search
duckduckgo-search  # ddgs
tavily-python

# Form parsing & file uploads
python-multipart

# Utilities
typing-extensions

# Optional: Piper TTS subprocess handling (no pip package needed)
# Piper binary must be installed separately if desired
# pip install piper-tts is not used; we call piper via subprocess

# Optional: for development/debugging
# structlog
