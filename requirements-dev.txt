# Development and testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # for async HTTP testing
ruff>=0.1.0   # linter (faster than flake8)
mypy>=1.7.0   # type checker (optional)
